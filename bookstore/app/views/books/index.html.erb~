<h1>Book Catalog:</h1>

<table>
  <thead>
    <tr>
      <th> title</th>
      <th>Condition</th>
      <th>Price</th>
      <th>Isbn</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @books.each do |book| %>
      <tr>
        <td><%= book. title %></td>
        <td><%= book.condition %></td>
        <td><%= book.price %></td>
        <td><%= book.isbn %></td>
        <% if session[:user_id] != nil %>
        	<td><%= link_to 'Order', book %></td>
	<% end %>
        <!--<td><%= link_to 'Edit', edit_book_path(book) %></td>-->
        <!--<td><%= link_to 'Destroy', book, method: :delete, data: { confirm: 'Are you sure?' } %></td>-->
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<% if session[:user_id] == nil %>
	<h4 class="emptyStoreMessage">You have not Logged in! Click the link below to Login:</h4>
	<h4><%= link_to 'Login', login_path %></h4>
<% else %>
	<h4><%= link_to 'Logout', logout_path %></h4>
<% end %>
<!--<%= link_to 'New Book', new_book_path %>-->



<!--
<h1>Books Store</h1>
<% @books.each do |book| %>
<dic class="entry">
<h3><%= book.title %></h3>
<h4><%= book.condition %></h4>
  <div class="price_line">
    <span class="price"><%= number_to_currency(book.price) %></span>
  
  </div>
</div>
<% end %>-->
